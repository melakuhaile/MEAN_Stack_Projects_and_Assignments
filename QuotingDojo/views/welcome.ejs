<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Welcome!</title>
</head>

<body>
    <% if(typeof(errors) != 'undefined' ) { %>
        <% for (var x in errors) { %>
            <h3><%= errors[x].message %></h3>
        <% } %>
    <% } %>
    <h1>Welcome to Dojo Quotes</h1>
    <form action="/quotes" method="post">
        Your Name:
        <input type="text" name='name'>
        Your Quote:
        <textarea name="quote" rows="6" cols="20"></textarea>
        <input type="submit" value="Add my quote!">
    </form>
    <a href="/quotes">Skip to quotes!</a>
   
</body>

</html>